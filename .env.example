NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Used by /api/revalidate to verify webhook calls before re-rendering.
# Set to a long random string and pass as ?secret=... when hitting the endpoint.
REVALIDATE_SECRET=change_me
